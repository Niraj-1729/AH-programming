#include <iostream>
using namespace std;

int getLargest(int arr[], int n)
{
    
}

int main()
{
    int arr[] = {10, 30, 50, 23, 12, 34};
    int n = sizeof(arr) / sizeof(arr[0]);
    getLargest(arr, n);
    return 0;
}