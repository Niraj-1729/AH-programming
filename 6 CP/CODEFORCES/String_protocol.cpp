#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;
        string protocol;
        cin >> protocol;
        char arr[n];
        for (int i = 0; i < n; i++)
        {
            arr[i] = protocol[i];
        }
        sort
        for (int  i = 0; i < n; i++)
        {
            if (arr[])
            {
                /* code */
            }
            
        }
        
    }
    return 0;
}
