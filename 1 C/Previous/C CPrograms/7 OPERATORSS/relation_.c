// == equal
// !=not eual to 
// <=less than or equal to 
// >=greater than or eual to 
// <less than
// >greater than 
// return only true or false 
// ? is used for not/

                    logical operators 
        && and (returns true if all is true and false when one or more than one is false )
        || or (true when one one or more than one is true and false when all conditions are false )
        ! not       (true when false and false enrn true)      
        used to combine sentences 

short circuit in case of &&(simply means if there is a condition anywhere in the expression that returns false then rest conditions wil not be executed )

